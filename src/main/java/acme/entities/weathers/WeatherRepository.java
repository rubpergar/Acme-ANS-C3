
package acme.entities.weathers;

/*
 * @Repository
 * public interface WeatherRepository extends AbstractRepository {
 * 
 * @Query("SELECT f from Flight f")
 * Collection<Flight> findAllFlights();
 * 
 * @Query("""
 * select count(w) > 0
 * from Weather w
 * where w.city = :city
 * and w.country = :country
 * and w.date = :date
 * """)
 * boolean existsDuplicateWeather(@Param("city") String city, @Param("country") String country, @Param("date") Date date);
 * 
 * }
 */
